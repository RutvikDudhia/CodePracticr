package Trees;

public class FindSuccessor {
	public void findSuccessor1(TreeNode treeNode)
	{/*
		TreeNode successor;
		TreeNode traverser;
		if(treeNode.rightChild != null)
			{
				successor = findleftMost(treeNode.right);
			}
			else if( treeNode == treeNode.parent.left)
			{
				successor = treeNode.parent;
			}
			else if(treeNode == treeNode.parent.right)
				{
					traverser = treeNode;

					while(traverser!== null)
					{
						
						
						if(traverser == traverser.parent.left)
							break;
						else
							
							traverser = traverser.parent;
					}
				if(travserser ! = null)	
					System.out.println("The succssor is " + 			traverser.parent.value);
				}
	}		*/
}
}
